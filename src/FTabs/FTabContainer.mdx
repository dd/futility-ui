import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as FTabContainerStories from './FTabContainer.stories';

<Meta of={FTabContainerStories} />


# FTabContainer

Tab container component. Declare tabs via `FTab` components in the default slot.

```js
import { FTabContainer, FTab } from 'futility-ui';
// or
import FTabContainer from 'futility-ui/FTabs/FTabContainer';
import FTab from 'futility-ui/FTabs/FTab';
```

```html
<FTabContainer v-model:activeTab="currentTab">
	<FTab name="Overview">Overview content</FTab>
	<FTab name="Settings">Settings content</FTab>
</FTabContainer>
```

Tabs render in declaration order. The first non-disabled tab activates by default.

> `FTabContainer` has multiple root elements (`header`, `hr`, `div`) — no extra wrapper needed, but attributes cannot be passed directly to the root.

<Canvas of={FTabContainerStories.Default} />
<Controls of={FTabContainerStories.Default} />


## Custom slugs

The slug is used as the active tab identifier in `v-model:activeTab`. It is auto-generated from `name` if omitted. Set `slug` explicitly to sync tabs with route params or control them programmatically.

```html
<FTabContainer v-model:activeTab="route.query.tab">
	<FTab name="General" slug="general">...</FTab>
	<FTab name="Security" slug="security">...</FTab>
</FTabContainer>
```

<Canvas of={FTabContainerStories.CustomSlugs} />


## Dynamic tabs

Add or remove `FTab` components at runtime — the header updates automatically. When the active tab is removed, the first available tab activates.

<Canvas of={FTabContainerStories.Dynamic} />


## Custom tab slot

Tab buttons in the header can be customized via named slots on `FTabContainer` using the pattern `tab-{slug}`. The slot exposes `{ tab }` with `name`, `slug`, and `disabled` fields.

```html
<FTabContainer v-model:activeTab="tab">
	<template #tab-inbox="{ tab }">
		{{ tab.name }}
		<span class="badge">3</span>
	</template>

	<FTab name="Inbox" slug="inbox">...</FTab>
	<FTab name="Sent" slug="sent">...</FTab>
</FTabContainer>
```

<Canvas of={FTabContainerStories.CustomTabSlot} />
